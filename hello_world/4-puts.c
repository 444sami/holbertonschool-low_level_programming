#include <stdio.h>
/**
 * main - entry point
 *
 * Return: always 0
 * wildcard backslash "\"" is used as an escape character,
 * to protect a subsequent special character
 * 
 */
int main (void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
