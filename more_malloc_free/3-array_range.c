#include "main.h"
/**
 * array_range - creates an array of integers
 * @min: all values from min
 * @max: all values from max
 * Return: NULL if min > max or if malloc fails,
 * otherwise return pointer to the new array
 */
int *array_range(int min, int max)
{

}
